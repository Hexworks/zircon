package org.hexworks.zircon.api.component

import org.hexworks.zircon.api.behavior.TitleHolder

/**
 * A [HBox] is a [Container] which automatically aligns
 * elements added to it horizontally (from top to bottom).
 */
interface HBox : Container, TitleHolder
