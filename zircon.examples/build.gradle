apply plugin: 'kotlin'

archivesBaseName = 'zircon.examples'

dependencies {
    compile project(':zircon.jvm.swing')
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
